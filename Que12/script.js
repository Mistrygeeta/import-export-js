//Create a function that takes an array of numbers, filters even numbers, and exports the result. Import it and log the result in another file.

function evenNum(arr){
    return  arr.filter(num=>num%2===0)
    

}

export default evenNum